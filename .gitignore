node_modules/
.next/
dist/
out/
.turbo/
coverage/
*.log
*.tsbuildinfo

# env — values live in Doppler; never commit a real .env
.env
.env.*
!.env.example

.DS_Store
.vercel
.data/
supabase/.temp/
supabase/.branches/

# Playwright output + local proof screenshots
test-results/
apps/web/.artifacts/

# Operating-state backups. Every file is a full copy of the production
# document, including driver names, contact details and (once the credential
# vault ships) licence and insurance data. A backup must never enter git.
backups/
.env*
