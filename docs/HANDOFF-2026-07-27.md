# LogLoads handoff — 2026-07-27

**From:** Claude (Opus 5), closed out by Codex · **To:** the next agent
**Branch:** `claude/launch-readiness` → **PR #70 is OPEN and NOT MERGED.** `main` is still `1b809b0`.

---

## 0a. CURRENT PR STATUS — START HERE

PR #70 is now locally green and review-clean, but it remains unmerged because repository policy requires
independent approval before `main`, and merging `main` deploys production.

The launch blockers originally recorded here are fixed:

1. Hosts can attach or replace a card in-product before publishing.
2. The authenticated daily scheduler materializes every missed closed month and walks the open invoice
   backlog oldest-first.
3. Credential review preserves provider-read expiry and fails closed on missing provider configuration.
4. Driver payment receipt survives a fee-ledger failure, and every scheduler run now reconciles received
   assignments missing their deterministic fee event before opening invoices. A repair that lands after
   an earlier bill opened gets a deterministic supplemental invoice; no provider-bound subtotal is
   rewritten.
5. Backup restore is version-gated and post-write verified.
6. An already-issued open Stripe invoice is retried without minting another provider invoice; a replay of
   an older paid bill cannot clear a newer payment failure.
7. An open bill is described as issued and outstanding rather than charged, and card replacement waits
   for the exact payment method confirmed by Stripe instead of accepting the old attached card.
8. Direct-offer acceptance refreshes the committed server projection, so remaining capacity and sibling
   assignment controls converge immediately after the local success state.
9. Direct offers without frozen stated driver pay are refused for reissue, and equal-second Stripe card
   and payment events converge through a deterministic event-id tie-break.
10. Fleet acceptance options now block only assignments whose haul windows actually overlap, so a booked
    trip on another day no longer hides an otherwise eligible rig while true double-booking still fails closed.
11. Confirmed delivery cannot be cancelled through either trip or assignment mutations, and a cancelled
    assignment cannot record the driver's payment receipt.
12. Driver-stated credential expiry now rejects impossible calendar dates instead of accepting JavaScript's
    normalized rollover into a later month.

Final local validation on the closeout source state:

- `pnpm validate`: **1,193 tests passed**, production build passed, all 10 migrations applied to a fresh
  local Supabase reset, and guardrails passed.
- `pnpm test:e2e`: **96 accounted — 77 passed, 19 intentional desktop guards skipped, 0 failed/retried**.
- Ten targeted revert probes turned red across the final service and billing-UI repairs, then returned
  green after restoration. The direct-offer refresh also passed the exact captured failure and the full
  clean-seed browser matrix.

Production activation remains deliberately incomplete: Vercel Production has no dedicated
`ANTHROPIC_API_KEY`; Stripe account/webhook ownership has not been provider-proven; the founder Clerk
admin allow-list and mill/destination write path are absent; and `LOGLOADS_FEE_COLLECTION` remains
disabled pending Stripe ownership proof and the broker/legal decision. No real charge was made.

**A secret was committed earlier and has been removed.** `apps/web/lib/media-config.test.ts` carried
Explore & Earn's live Cloudinary API key in a fixture. The value is now a placeholder, but it remains in
git history: **the Explore & Earn owner should rotate that key.** The paired secret was never committed.

---

## 0b. READ THIS BEFORE YOU EXECUTE ANYTHING

This is not a formality. A large amount of work landed in the last 24 hours, and the fastest way to
waste a day is to rebuild something that already exists or to "fix" something that is deliberate.

**Required before your first edit:**

1. `git fetch && git log --oneline -8` on `main`. Everything below is already merged.
2. Read §2 (locked decisions), §4 (what exists), §6 (traps). §6 is the part that will actually bite you.
3. Run `pnpm typecheck && pnpm test` and confirm you see **1,176 package tests + 16 tool tests, 0 failing**.
   If you see a different number, find out why before writing code.
4. `ae start logloads -t <your-task> -a <you>` before any write. One writer per repo.
5. Do not re-audit. `LogLoads-Comprehensive-Audit-2026-07-25.md` (with the founder) is the evidence base;
   ~40% of its findings are now fixed and marked below.

**If you disagree with a locked decision in §2, raise it with the founder. Do not quietly implement
something else.**

---

## 1. What LogLoads is

A timber-hauling marketplace. A **host** posts loads; a **driver** hauls them. Hosts may add dispatchers,
fleet managers and billing users. Pre-customer: production has real infrastructure and **zero real users**.

---

## 2. LOCKED founder decisions — do not re-litigate

### Pricing (confirmed 2026-07-27)

| | |
|---|---|
| Host fee | **5% of the driver pay the host states**, charged **on completed loads only** |
| Monthly minimum | **NONE.** There is no $49/mo minimum. It was considered and explicitly dropped. Do not reintroduce it |
| Charge to post | None. No monthly host fee |
| Drivers | **Free forever** |
| Fee direction | Charged to the host **on top**. **Never** deducted from driver pay — the driver receives exactly the stated figure |
| Collection | Card on file **required before a host can publish**; charged **monthly in arrears** |
| Stripe mechanism | **Ad-hoc invoice items** on a Customer with a default payment method. Deliberately not a Price or Subscription, so nothing needs creating in the Stripe dashboard |
| Dispatch Pro | $499/mo for fleets, retained, unrelated to the load fee. Optimise, don't remove |

`PLATFORM_FEE_BPS = 500` in `packages/contracts/src/billing-model.ts` is the single source. Nothing else
computes a fee.

### Other locked decisions

- **Strictly non-custodial.** LogLoads never holds, escrows, routes or pays out driver funds. Money moves
  host → driver directly, outside the platform. LogLoads may record payment status only. A guardrail bans
  Connect/transfer tokens — do not add payout code.
- **Credentials are mandatory and binding**: insurance, CDL, truck, trailer. AI reviews them; anything
  short of approved **blocks load acceptance**. Expiry blocks too.
- **The host receives status + expiry + truck/trailer photos only** — never the CDL or insurance image.
- **Single JSONB row store: HARDEN, do not migrate.** Committed to deliberately (§3).
- **Media: Supabase Storage** (decided 2026-07-27, replacing Cloudinary). Reasons in §5.
- LogLoads is **orchestration**. Never claim it has verified anyone's legal right to operate.

---

## 3. Architecture you must understand before touching anything

**The entire database is ONE JSONB row**: `public.operating_state` id=`'primary'`, read and written whole
under a `version` compare-and-swap, using the service-role key.

There are 38 relational tables and 39 RLS policies. **They are empty and unread.** `load_postings`,
`profiles` and `companies` all have 0 rows in production. Verified by direct query.

Consequences you cannot design around:

- **No unique index, no foreign key, no CHECK constraint.** Deterministic ids plus explicit assertions are
  the only integrity. Every guard needs a test that fails when the guard is removed.
- **Every check must run inside the same mutation as its write.** A check before `mutateState` is defeated
  by a CAS retry.
- **Application TypeScript is the only security boundary.** RLS contributes nothing at runtime.
- **Rows are now zod-parsed on read** (`packages/db/src/snapshot.ts`). `ROW_VALIDATORS` is an exhaustive
  `Record<LogLoadsTableName, RowValidator>` — **a new collection will not compile until you declare its
  validator**, and it needs an additive `?? []` backfill or the first production read refuses the whole
  document and every request fails.
- A malformed row is **withheld, not dropped** — withheld rows travel on the snapshot and are re-appended
  on write, because in a write-whole store dropping a row means the next CAS erases it from Postgres.

---

## 4. What already exists — do not rebuild

### Implemented in this branch (validated, pending PR merge)

| Area | State |
|---|---|
| **Driver-facing pay** | Fixed. `economics.ts` reads `driverPayCents`; a stated figure is used as-is. Wording lives in `pay-display.ts` — one module, not nine call sites |
| **Host states driver pay** | Required field in the publish builder, threaded through and persisted. Had **no write path at all** before |
| **5% fee ledger** | `billing-model.ts` (pure) + `platform-fees.ts`. Deterministic event ids, at most one per assignment, `driverPayCents`/`feeBps` frozen per event |
| **Fee accrual wiring** | Host records payment sent, the assigned driver confirms the amount received, and only that distinct-person receipt state accrues the frozen 5% fee |
| **Payment method gate** | Host cannot publish without an attached card; the Stripe Payment Element lets the host attach or replace it in-product |
| **Stripe** | Customer, SetupIntent, monthly invoice opening/charging cron, webhook signature verification and idempotency. Real collection remains behind `LOGLOADS_FEE_COLLECTION` |
| **Credential vault** | `credentials.ts` (pure) + `driver-credentials.ts` + `CredentialVault.tsx`. Submit, AI review, append-only decision trail, renewal via supersede |
| **Credential gate** | Binding, on **both** assignment paths (capacity request **and** direct-offer claim via `finalizeCapacityAssignment`) |
| **AI reviewer** | `credential-reviewer.ts`. Zod-validates model output; **fails closed on every error path**. `ANTHROPIC_API_KEY` is a required hauling activation dependency and is currently absent from Vercel Production |
| **Security** | 5 critical defects closed with revert probes: truck-slots cross-org read, availability cross-driver overwrite, `createThread` authz, error-message oracle, rate-limit key |
| **Store hardening** | Row validation + non-destructive quarantine, +1.2 ms/request measured |
| **Backups** | `tools/operating-state-backup.mjs` + `docs/RECOVERY.md`. **Rehearsed**: 386 rows → destroyed → recovered |
| **Private media** | Supabase private bucket, signed browser upload, server read-back validation, signed delivery, and fail-closed project/bucket tenancy checks |
| **Billing scheduler** | Daily Vercel cron reconciles missing fee events, materializes every missed closed month, opens deterministic supplemental recovery bills without changing provider-bound amounts, then idempotently dark-runs or charges the entire open backlog oldest-first according to the fee gate |
| **Grants** | `20260726090000_revoke_public_relational_grants.sql` applied to production; anon/authenticated write and target-function EXECUTE grants are absent |
| **Pricing copy** | 9 surfaces corrected from "Free launch pilot" to the real 5% terms |

### Still true from the audit — NOT fixed

- `ANTHROPIC_API_KEY` is absent from Vercel Production, so every credential review
  fails closed and no driver can accept work until a dedicated LogLoads key and
  real-document round trip are provider-verified
- No platform admin can exist in production (seed-only `clerkUserId: "clerk-admin-1"`)
- Mills have no write path → a host outside two seeded Oregon mills can never build a lane, so never publish
- No member removal, role change or suspension anywhere
- Non-billing expiry notices, dunning, request timeouts, and the 72h rule still
  need scheduled handlers even though the billing cron now provides the runner
- An unapproved capacity request consumes capacity and flips the load to `filled` with no timeout
- Two contradictory state-machine generations both live in `operating-network.ts`
- `requestAssignment` is an exported unguarded writer that bypasses the normal checks

---

## 5. Next tasks, in order

1. Independent review and merge of PR #70, followed by exact-SHA production
   smoke, authenticated private-media round trip, and signup/onboarding proof.
2. Provision a dedicated LogLoads `ANTHROPIC_API_KEY`, verify the configured
   credential-review model, and complete one real private-document review from
   signed upload through stored decision and assignment eligibility.
3. Prove the configured Stripe account is dedicated to LogLoads and complete one
   live SetupIntent plus signed webhook round trip. Keep
   `LOGLOADS_FEE_COLLECTION=disabled` until that evidence and legal approval
   exist; then enable it deliberately.
4. Platform admin reachability (allow-list founder Clerk ids via env).
5. Mills/destinations write path.
6. Scheduled expiry notices, dunning, request timeout, and 72h handlers.
7. Member removal / role change / suspension.
8. Operational email beyond the contact form, after the sender domain is
   independently verified.

---

## 6. Traps — read this section twice

- **Check that new code is CALLED, not just that it exists.** The fee accrual service was built, tested,
  exported and bound on the services facade with **zero call sites**. A fee engine nothing calls earns
  nothing. `fee-accrual-wiring.test.ts` exists solely because of this. Grep for call sites before believing
  a feature works.
- **Seed changes never reach production.** The canonical row is only created when none exists, so
  production still holds an early seed (6 loads, 11 profiles, cas version 3). `/api/health`'s
  `engine.profiles: 11` is **seed personas**, not business data. Do not use it as a metric.
- **On deploy, production's publish and request paths will refuse.** The new collections backfill to `[]`,
  so no seeded host has a billing profile and no driver has credentials. That is the gates working
  correctly, not an outage — nothing errors, it refuses. Refresh the canonical state or create records when
  you want the demo working again.
- **The migration ledger was reconciled on 2026-07-27.** The three pending repo
  migrations were applied idempotently and the canonical row hash was unchanged.
  Future migrations still require a pre-write backup/hash, exact migration
  inventory, and post-write state verification.
- **CI has no Supabase credentials.** That is why the required `migrations` check never touches a real
  database and the drift went unnoticed. Backups cannot be scheduled until secrets exist.
- **The e2e suite is NOT a required status check**, and `enforce_admins` is false. A PR with every browser
  journey red is mergeable, and merging `main` auto-deploys production. Fix this early.
- **Two existing tests asserted the defects they covered** (`rate-limit-client-key.test.ts` pinned the
  shared-bucket constants; `economics.test.ts` pinned the rate-card derivation). Expect more of this. When a
  test blocks a correct fix, rewrite it deliberately and say so — do not weaken it to go green.
- **`information_schema.role_table_grants` is not the authority on privileges.** It reported `anon:TRUNCATE`
  after the revoke had worked, because those rows were PostGIS *views*. Use `has_table_privilege`.
- **Objects are updated immutably.** A test holding a reference from before a mutation is asserting against
  a stale copy. Re-read from state.
- **Hand-computed constants belong in tests, and they will catch you.** A degree of latitude is 69.09409
  miles, not 69.0932 — the absolute assertion caught the wrong constant immediately.
- **Parallel agents must own disjoint files.** Two agents editing one file corrupts work. Partition by file
  and state ownership explicitly.
- Local e2e flakes under load; only clean-runner CI is trustworthy. `pnpm build` races a running dev server.
  Playwright reuses an existing server on :3002 — kill the bench first or you test the dev server.

---

## 7. Blocked on the founder — none of this is engineering work

| Item | Consequence while missing |
|---|---|
| Founder's Clerk user id | No platform admin can exist; nothing is administrable |
| Confirm Resend domain verified | Operational email stays in-app only |
| Supabase + CI secrets | No scheduled backups; migrations never validated against a real environment |

**Legal, still open and outside engineering:** a 5% fee for matching shippers to carriers engages the
federal broker definition that the site footer disclaims on every page. Counsel must rule before the first
charge.

---

## 8. How to know you have not broken anything

```bash
pnpm typecheck && pnpm test && pnpm lint && pnpm guardrails
```

Expect **1,176 package + 16 tool tests (1,192 total), 0 failing, 0 skipped**. Then `pnpm validate` and
`pnpm test:e2e` (96 accounted; 19 skipped are documented desktop-chrome guards).

Finish with `ae finish logloads -a <you>` — work counts only when pushed and remote-verified.

---

## 9. Standing rules

- Every fix needs a test that **fails when the fix is reverted**. Verify by actually reverting it.
- Comments say **why**. A comment asserting something untrue is a defect — one was found in a security
  migration this week.
- Derive option lists from schemas; never hand-copy domain values.
- Do not fabricate data, approvals, confidences or financial records in seeds. One seeded driver is cleared;
  that is deliberate, and the reason is in the comment.
- Report honestly. If something is not done, say so. The audit this work came from exists because
  "complete" had been reported for things that were never wired.
