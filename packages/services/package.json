{
  "name": "@logloads/services",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint src --ext .ts --max-warnings=0",
    "test": "vitest run --passWithNoTests --maxWorkers=50% --testTimeout=15000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@logloads/contracts": "workspace:*",
    "@logloads/db": "workspace:*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.13.2"
  }
}
